.DS_Store
~$*
*.tmp

# secrets
.env
backend/.env
frontend/.env.local
demo/secrets/
*.pem
mcp_key
CH56655-api2026hack22.zip
# Gmail OAuth — never commit credentials/tokens
.gmail_token.json
.gmail_client_secret.json
backend/.gmail_token.json
backend/.gmail_client_secret.json
.gmail_history.json
backend/data/.gmail_history.json

# python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# node
node_modules/
.next/
frontend/out/

# caches
/cache/
backend/.cache/

# RM capture write-through store (replayed on boot)
backend/data/captured_entries.json

# SQLite store (RM accounts + briefing log)
backend/data/workbench.db
backend/data/workbench.db-shm
backend/data/workbench.db-wal

# Front Door kanban board write-through store (replayed on boot)
backend/data/tasks.json
