/** AW logo square in the sidebar brand strip (`Sidebar` → `h-7 w-7`). */
export const DESK_BRAND_MARK_HEIGHT = "h-7";

/** Top inset for every main pane — aligns content below the sidebar product mark. */
export const MAIN_CONTENT_TOP_PADDING = "pt-7";
